# B站视频下载功能移植可行性分析

## 一、参考项目下载功能实现方案分析

### 1.1 项目架构概述

参考项目（Bili23-Downloader）是一个基于Python + PySide6（Qt）的桌面应用程序，采用模块化架构设计，主要包含以下核心模块：

- **下载器模块**（`src/util/download/downloader/`）
  - `downloader.py`: 核心下载器实现
  - `manager.py`: 下载器管理器
  - `merger.py`: 视频/音频合并器
  - `parse_worker.py`: 解析下载信息的工作线程

- **任务管理模块**（`src/util/download/task/`）
  - `info.py`: 任务信息数据结构
  - `manager.py`: 任务管理器
  - `db.py`: 数据库管理

- **解析模块**（`src/util/parse/`）
  - `episode/`: 视频信息解析
  - `parser/`: URL解析器
  - `preview/`: 预览信息解析

- **网络模块**（`src/util/network/`）
  - `request.py`: 网络请求封装
  - `download_url.py`: 下载URL探测
  - `cdn.py`: CDN选择

### 1.2 关键API接口分析

#### 1.2.1 视频信息获取API

**投稿视频（UGC）**
```
URL: https://api.bilibili.com/x/player/wbi/playurl
参数:
  - bvid: 视频BV号
  - cid: 视频CID
  - qn: 画质ID（200为自动选择）
  - fnver: 0
  - fnval: 4048（支持DASH、HDR、4K等）
  - fourk: 1
  - 需要WBI签名
```

**番剧/影视剧（PGC）**
```
URL: https://api.bilibili.com/pgc/player/web/playurl
参数:
  - bvid: 视频BV号
  - cid: 视频CID
  - qn: 画质ID
  - fnver: 0
  - fnval: 12240（支持所有格式）
  - fourk: 1
```

**课程视频（Cheese）**
```
URL: https://api.bilibili.com/pugv/player/web/playurl
参数:
  - avid: 视频AVID
  - cid: 视频CID
  - ep_id: 剧集ID
  - qn: 画质ID
  - fnver: 0
  - fnval: 16
  - fourk: 1
```

#### 1.2.2 音频信息获取API

```
URL: https://www.bilibili.com/audio/music-service-c/web/url
参数:
  - sid: 音频SID
  - privilege: 2
  - quality: 2
```

### 1.3 下载地址解析流程

#### 1.3.1 核心解析流程

```
1. 根据视频类型调用对应API
   ↓
2. 解析返回的JSON数据，确定媒体类型
   - DASH（m4s分离的视频/音频）
   - MP4（传统MP4格式）
   - FLV（老式FLV格式）
   ↓
3. 提取可用的画质、音质、编码列表
   ↓
4. 根据用户选择或优先级配置确定目标画质/音质/编码
   ↓
5. 构建下载列表
   - DASH: 分别构建视频和音频下载任务
   - MP4/FLV: 可能包含多个分段
   ↓
6. 对每个下载URL进行CDN探测
   - 发送HEAD请求获取文件大小
   - 验证URL有效性
   - 选择最佳CDN节点
   ↓
7. 返回下载信息（URL、文件大小、文件名等）
```

#### 1.3.2 关键代码实现

**视频信息解析**（`VideoInfoParser`）：
```python
class VideoInfoParser:
    def parse_info(self):
        # 1. 获取可用的画质列表
        available_quality_list = self.get_available_quality_list()

        # 2. 根据优先级或用户选择确定画质
        video_info = self.get_video_info(
            self.task_info.Download.video_quality_id,
            self.task_info.Download.video_codec_id
        )

        # 3. 根据媒体类型构建下载信息
        match self.task_info.Download.media_type:
            case MediaType.DASH:
                return self.make_dash_video_info(video_info, ext)
            case MediaType.MP4 | MediaType.FLV:
                return self.make_mp4_video_info(video_info, ext)
```

**CDN探测**（`resolve_download_url`）：
```python
def resolve_download_url(url_list: list[str], min_file_size: int = 1024):
    # 1. 遍历所有CDN URL
    for url in CDN.get_url_list(url_list):
        # 2. 发送HEAD请求探测文件大小
        file_size = _probe_url(url, min_file_size, deadline)

        # 3. 验证文件大小是否符合预期
        if file_size > min_file_size:
            return {"url": url, "file_size": file_size}

    raise RuntimeError("无法获取有效的下载链接")
```

### 1.4 画质音质编码选择实现

#### 1.4.1 支持的画质列表

```python
video_quality_map = {
    "auto": 200,          # 按优先级自动选择
    "8K": 127,            # 8K 超高清
    "DOLBY_VISION": 126,  # 杜比视界
    "HDR": 125,           # HDR 真彩
    "4K": 120,            # 4K 超高清
    "1080P60": 116,       # 1080P 60帧
    "1080P+": 112,        # 1080P 高码率
    "AI": 100,            # 智能修复
    "1080P": 80,          # 1080P 高清
    "720P": 64,           # 720P 准高清
    "480P": 32,           # 480P 标清
    "360P": 16            # 360P 流畅
}
```

#### 1.4.2 支持的音质列表

```python
audio_quality_map = {
    "auto": 30300,        # 按优先级自动选择
    "HI_RES": 30251,      # Hi-Res 无损
    "DOLBY_ATMOS": 30250, # 杜比全景声
    "192K": 30280,        # 192 kbps
    "132K": 30232,        # 132 kbps
    "64K": 30216          # 64 kbps
}
```

#### 1.4.3 支持的编码列表

```python
video_codec_map = {
    "auto": 20,           # 按优先级自动选择
    "AVC/H.264": 7,       # H.264编码
    "HEVC/H.265": 12,     # H.265编码
    "AV1": 13             # AV1编码
}
```

#### 1.4.4 选择逻辑

**自动选择模式**：
```python
def get_video_quality_id_by_priority(self):
    # 按配置的优先级顺序尝试
    for quality_id in config.get(config.video_quality_priority):
        if quality_id in self.video_info_map.keys():
            return quality_id
```

**手动选择模式**：
```python
def get_video_info(self, video_quality_id: int, video_codec_id: int):
    # 1. 检查用户选择的画质是否可用
    if video_quality_id not in available_quality_list:
        video_quality_id = available_quality_list[0]  # 降级到最高可用画质

    # 2. 检查用户选择的编码是否可用
    if video_codec_id not in available_codec_list:
        video_codec_id = available_codec_list[0]  # 降级到第一个可用编码

    return self.video_info_map[video_quality_id][video_codec_id]
```

### 1.5 下载任务管理

#### 1.5.1 任务数据结构

```python
@dataclass
class TaskInfo:
    Basic: BasicInfo        # 基础信息（任务ID、标题、创建时间等）
    File: FileInfo          # 文件信息（路径、扩展名等）
    Episode: EpisodeInfo    # 剧集信息（AVID、BVID、CID、标题等）
    Download: DownloadInfo  # 下载信息（状态、进度、速度等）
```

#### 1.5.2 任务状态管理

```python
class DownloadStatus:
    QUEUED = 0              # 排队中
    PARSING = 1             # 解析中
    DOWNLOADING = 2         # 下载中
    PAUSED = 3              # 已暂停
    MERGING = 4             # 合并中
    ADDITIONAL_PROCESSING = 5  # 附加文件处理中
    FFMPEG_QUEUED = 6       # 等待FFmpeg处理
    FFMPEG_FAILED = 7       # FFmpeg处理失败
    FAILED = 8              # 下载失败
    COMPLETED = 9           # 已完成
```

#### 1.5.3 数据库存储

- 使用SQLite数据库存储任务信息
- 支持任务的持久化存储
- 支持断点续传（通过保存已下载的分片信息）

### 1.6 下载器核心实现

#### 1.6.1 分片下载机制

```python
class ChunkWorker(QRunnable):
    def run(self):
        # 1. 构建Range请求
        headers = {"Range": f"bytes={self.chunk_range[0]}-{self.chunk_range[1] - 1}"}

        # 2. 流式下载
        with open(self.file_path, "r+b") as f:
            f.seek(self.chunk_range[0])

            with self.session.stream("GET", self.url, headers=headers) as response:
                for chunk in response.iter_bytes(chunk_size=8192):
                    # 3. 限速控制
                    if self.token_bucket:
                        self.token_bucket.consume(len(chunk), self.stop_event)

                    # 4. 写入文件
                    f.write(chunk)

                    # 5. 更新进度
                    with self.lock:
                        self.task_info.Download.downloaded_size += len(chunk)
```

#### 1.6.2 下载流程

```
1. 创建下载任务
   ↓
2. 解析下载信息（获取URL、文件大小等）
   ↓
3. 计算分片列表（默认4MB一个分片）
   ↓
4. 为每个分片启动下载线程
   ↓
5. 下载完成后通知主线程
   ↓
6. 所有文件下载完成后进行合并（如需要）
   ↓
7. 处理附加文件（弹幕、字幕、封面等）
   ↓
8. 标记任务完成
```

#### 1.6.3 错误处理与重试

- 支持自动重试（最多5次）
- 区分可重试错误和永久错误
- 可重试错误：408, 429, 500, 502, 503, 504等
- 永久错误：400, 401, 403, 404, 405, 410, 416等

### 1.7 FFmpeg合并功能

#### 1.7.1 合并流程

```python
class Merger:
    def run(self):
        # 1. 检查是否需要合并
        if not self.task_info.Download.merge_video_audio:
            return

        # 2. 构建FFmpeg命令
        cmd = [
            "ffmpeg",
            "-i", video_file,
            "-i", audio_file,
            "-c", "copy",
            "-y", output_file
        ]

        # 3. 执行合并
        process = subprocess.Popen(cmd, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
        process.wait()

        # 4. 检查结果
        if process.returncode != 0:
            self.task_info.Download.status = DownloadStatus.FFMPEG_FAILED
```

---

## 二、当前项目结构分析

### 2.1 项目架构

当前项目是一个Android TV应用，基于反编译和二次开发，主要特点：

- **编程语言**: Java + Smali混合代码
- **最低支持**: Android 4.x
- **UI框架**: Android原生UI + RecyclerView
- **网络库**: OkHttp3 + Retrofit
- **播放器**: IjkPlayer + ExoPlayer双引擎

### 2.2 核心模块

#### 2.2.1 视频详情页

- **路径**: `com/bilibili/tv/ui/video/VideoDetailActivity.java`
- **功能**:
  - 显示视频信息（标题、封面、UP主等）
  - 显示分P列表、合集列表
  - 提供播放、收藏、稍后再看等操作
  - 支持PGC和UGC视频

#### 2.2.2 播放器

- **路径**: `com/bilibili/tv/ui/video/player/`
- **功能**:
  - 视频播放（支持IjkPlayer和ExoPlayer）
  - 播放控制（播放/暂停/快进/快退）
  - 画质/音质切换
  - 弹幕显示

#### 2.2.3 设置页面

- **路径**: `com/bilibili/tv/ui/setting/SettingActivity.java`
- **功能**:
  - 左侧导航菜单
  - 右侧设置内容区域
  - 支持多种设置项

#### 2.2.4 API服务

- **路径**: `com/bilibili/tv/api/`
- **关键类**:
  - `VideoApiService`: 视频API
  - `BangumiApiService`: 番剧API
  - `BiliApiService`: 通用API

### 2.3 现有网络请求能力

- 已有Retrofit + OkHttp3网络框架
- 已有Cookie管理和认证机制
- 已有WBI签名实现（`WbiSigner.java`）
- 已有视频信息获取API（`VideoApiParser`等）

---

## 三、移植可行性分析

### 3.1 技术可行性评估

#### 3.1.1 API接口复用

**✅ 可直接复用**：
- 视频信息获取API（已存在`VideoApiParser`等）
- WBI签名机制（已存在`WbiSigner`）
- Cookie管理（已存在`CookieUtil`）

**⚠️ 需要新增**：
- 下载URL解析逻辑（DASH、MP4、FLV格式解析）
- CDN探测和选择逻辑
- 画质/音质/编码选择逻辑

#### 3.1.2 下载功能实现

**✅ 可行**：
- Android原生支持HTTP下载（使用OkHttp）
- 支持分片下载（Range请求）
- 支持断点续传（本地保存进度）
- 支持多线程下载（线程池）

**⚠️ 需要注意**：
- Android TV存储权限管理
- 下载文件路径选择（外置存储）
- 下载任务的后台执行（Service）
- 下载通知和进度更新

#### 3.1.3 视频合并功能

**✅ 可行**：
- 可以使用FFmpeg Android库
- 或者直接下载已合并的MP4格式（避免合并）

**⚠️ 需要注意**：
- FFmpeg库体积较大（~20MB）
- 需要考虑CPU架构（arm, x86等）
- 合并过程耗时，需要后台处理

#### 3.1.4 数据库存储

**✅ 可行**：
- Android原生支持SQLite
- 可使用Room或原生SQLite
- 需要设计数据库表结构

### 3.2 功能实现方案

#### 3.2.1 在视频详情页添加下载功能

**实现步骤**：

1. **UI改造**：
   - 在视频详情页添加"下载"按钮（可放在收藏按钮旁边）
   - 支持批量选择分P视频下载
   - 点击后弹出下载选项对话框

2. **下载选项对话框**：
   - 选择画质（自动、8K、4K、1080P等）
   - 选择音质（自动、无损、高音质等）
   - 选择编码（自动、H.264、H.265、AV1）
   - 选择是否下载弹幕、字幕、封面
   - 显示预估文件大小

3. **创建下载任务**：
   ```java
   public class DownloadTask {
       private String taskId;           // 任务ID
       private String title;            // 视频标题
       private String bvid;             // BV号
       private long cid;                // CID
       private int videoQualityId;      // 画质ID
       private int audioQualityId;      // 音质ID
       private int videoCodecId;        // 编码ID
       private String downloadPath;     // 下载路径
       private long totalSize;          // 总大小
       private long downloadedSize;     // 已下载大小
       private int status;              // 状态
       private int progress;            // 进度
       private long createTime;         // 创建时间
       private long completeTime;       // 完成时间
   }
   ```

4. **调用API获取下载信息**：
   ```java
   // 复用现有的VideoApiParser或创建新的DownloadApiParser
   public class DownloadApiParser {
       public DownloadInfo parseDownloadUrl(String bvid, long cid, int qualityId) {
           // 调用 playurl API
           // 解析返回的JSON
           // 返回下载信息
       }
   }
   ```

#### 3.2.2 下载队列管理页面

**实现方案**：

1. **新建Activity**: `DownloadManagerActivity`
   - 继承自`BaseUpViewActivity`（与SettingActivity类似）
   - 左侧导航：下载中、已完成
   - 右侧列表：显示下载任务

2. **下载中任务列表**：
   - 显示任务标题、进度、速度、状态
   - 支持暂停、继续、删除操作
   - 支持批量操作

3. **已完成任务列表**：
   - 显示任务标题、文件大小、完成时间
   - 支持播放、删除、打开文件夹操作

4. **下载服务**: `DownloadService`
   - 在后台执行下载任务
   - 使用线程池管理并发下载
   - 支持断点续传
   - 发送通知更新进度

#### 3.2.3 播放已下载视频

**实现方案**：

1. **在播放器启动时检查**：
   ```java
   public class PlayerActivity extends BaseActivity {
       @Override
       protected void onCreate(Bundle savedInstanceState) {
           // 检查是否有已下载的视频
           String bvid = getIntent().getStringExtra("bvid");
           long cid = getIntent().getLongExtra("cid", 0);

           DownloadTask task = DownloadManager.getInstance().getCompletedTask(bvid, cid);
           if (task != null) {
               // 播放本地文件
               playLocalFile(task.getFilePath());
           } else {
               // 播放在线视频
               playOnlineVideo(bvid, cid);
           }
       }
   }
   ```

2. **本地文件播放**：
   - 使用IjkPlayer或ExoPlayer播放本地文件
   - 支持所有在线播放的功能（弹幕、字幕等）

#### 3.2.4 启动时自动继续下载

**实现方案**：

1. **在Application启动时检查**：
   ```java
   public class MainApplication extends Application {
       @Override
       public void onCreate() {
           super.onCreate();

           // 启动下载服务
           startService(new Intent(this, DownloadService.class));

           // 恢复未完成的下载任务
           DownloadManager.getInstance().resumeUnfinishedTasks();
       }
   }
   ```

2. **恢复下载逻辑**：
   ```java
   public class DownloadManager {
       public void resumeUnfinishedTasks() {
           // 从数据库查询状态为"下载中"的任务
           List<DownloadTask> tasks = database.getDownloadingTasks();

           for (DownloadTask task : tasks) {
               if (task.getStatus() != DownloadStatus.PAUSED) {
                   // 继续下载
                   startDownload(task);
               }
           }
       }
   }
   ```

#### 3.2.5 在设置中增加下载管理

**实现方案**：

1. **在SettingActivity中添加新菜单项**：
   - 菜单标题："下载管理"
   - 点击后进入下载设置页面

2. **下载设置页面**：
   - **保存位置设置**：
     - 显示当前保存路径
     - 支持选择外置存储路径
     - 显示可用空间

   - **画质设置**：
     - 默认画质选择（自动、最高画质、指定画质）
     - 画质优先级设置

   - **音质设置**：
     - 默认音质选择
     - 音质优先级设置

   - **编码设置**：
     - 默认编码选择
     - 编码优先级设置

   - **其他设置**：
     - 同时下载数量（1-3）
     - 下载限速（0为不限速）
     - 仅在WiFi下载
     - 下载完成后通知

### 3.3 核心类设计

#### 3.3.1 下载管理器

```java
public class DownloadManager {
    private static DownloadManager instance;
    private SQLiteDatabase database;
    private ExecutorService executorService;
    private Map<String, DownloadTask> activeTasks;

    // 创建下载任务
    public void createTask(String bvid, long cid, DownloadOptions options) {
        // 1. 检查是否已存在
        // 2. 创建TaskInfo
        // 3. 保存到数据库
        // 4. 添加到下载队列
    }

    // 开始下载
    public void startDownload(String taskId) {
        // 1. 从数据库加载任务信息
        // 2. 创建DownloadWorker
        // 3. 提交到线程池
    }

    // 暂停下载
    public void pauseDownload(String taskId) {
        // 1. 设置停止标志
        // 2. 保存当前进度到数据库
        // 3. 从活动任务列表移除
    }

    // 删除任务
    public void deleteTask(String taskId) {
        // 1. 停止下载
        // 2. 删除数据库记录
        // 3. 删除已下载的文件
    }

    // 查询下载中任务
    public List<DownloadTask> getDownloadingTasks() {
        return database.queryDownloadingTasks();
    }

    // 查询已完成任务
    public List<DownloadTask> getCompletedTasks() {
        return database.queryCompletedTasks();
    }

    // 获取已完成的本地文件路径
    public String getLocalFilePath(String bvid, long cid) {
        DownloadTask task = database.getCompletedTask(bvid, cid);
        return task != null ? task.getFilePath() : null;
    }
}
```

#### 3.3.2 下载工作线程

```java
public class DownloadWorker implements Runnable {
    private DownloadTask task;
    private volatile boolean isPaused = false;
    private OkHttpClient client;

    @Override
    public void run() {
        try {
            // 1. 解析下载URL
            DownloadInfo info = parseDownloadUrl(task);

            // 2. 创建临时文件
            File tempFile = new File(task.getDownloadPath(), task.getFileName() + ".tmp");

            // 3. 分片下载
            long downloadedSize = task.getDownloadedSize();
            long totalSize = info.getFileSize();

            Request request = new Request.Builder()
                .url(info.getUrl())
                .header("Range", "bytes=" + downloadedSize + "-")
                .build();

            Response response = client.newCall(request).execute();

            InputStream inputStream = response.body().byteStream();
            RandomAccessFile outputFile = new RandomAccessFile(tempFile, "rw");
            outputFile.seek(downloadedSize);

            byte[] buffer = new byte[8192];
            int bytesRead;

            while ((bytesRead = inputStream.read(buffer)) != -1 && !isPaused) {
                outputFile.write(buffer, 0, bytesRead);
                downloadedSize += bytesRead;

                // 更新进度
                task.setDownloadedSize(downloadedSize);
                task.setProgress((int) (downloadedSize * 100 / totalSize));

                // 通知UI更新
                notifyProgressUpdate(task);
            }

            if (!isPaused) {
                // 下载完成，重命名文件
                File finalFile = new File(task.getDownloadPath(), task.getFileName());
                tempFile.renameTo(finalFile);

                task.setStatus(DownloadStatus.COMPLETED);
                task.setCompleteTime(System.currentTimeMillis());

                // 保存到已完成数据库
                DownloadManager.getInstance().markTaskCompleted(task);
            }

        } catch (Exception e) {
            task.setStatus(DownloadStatus.FAILED);
            // 保存失败状态
            DownloadManager.getInstance().updateTaskStatus(task);
        }
    }

    public void pause() {
        isPaused = true;
    }
}
```

#### 3.3.3 下载信息解析器

```java
public class DownloadUrlParser {
    public DownloadInfo parseVideoDownloadUrl(String bvid, long cid, int qualityId) {
        // 1. 构建请求参数
        Map<String, Object> params = new HashMap<>();
        params.put("bvid", bvid);
        params.put("cid", cid);
        params.put("qn", qualityId);
        params.put("fnver", 0);
        params.put("fnval", 4048);  // 支持DASH
        params.put("fourk", 1);

        // 2. WBI签名
        String signedUrl = WbiSigner.sign("https://api.bilibili.com/x/player/wbi/playurl", params);

        // 3. 发送请求
        String json = OkHttpUtil.get(signedUrl);

        // 4. 解析JSON
        JSONObject data = JSON.parseObject(json).getJSONObject("data");

        DownloadInfo info = new DownloadInfo();

        // 5. 判断格式
        if (data.containsKey("dash")) {
            // DASH格式
            info.setMediaType("DASH");
            parseDashInfo(data.getJSONObject("dash"), info);
        } else {
            // MP4/FLV格式
            String format = data.getString("format");
            info.setMediaType(format);
            parseMp4Info(data, info);
        }

        return info;
    }

    private void parseDashInfo(JSONObject dash, DownloadInfo info) {
        // 解析视频流
        JSONArray videoArray = dash.getJSONArray("video");
        List<VideoStreamInfo> videoStreams = new ArrayList<>();

        for (int i = 0; i < videoArray.size(); i++) {
            JSONObject video = videoArray.getJSONObject(i);
            VideoStreamInfo streamInfo = new VideoStreamInfo();
            streamInfo.setQualityId(video.getInteger("id"));
            streamInfo.setCodecId(video.getInteger("codecid"));

            // 解析baseUrl
            JSONArray baseUrlArray = video.getJSONArray("baseUrl");
            streamInfo.setUrls(parseUrlList(baseUrlArray));

            videoStreams.add(streamInfo);
        }

        info.setVideoStreams(videoStreams);

        // 解析音频流（类似视频流）
        // ...
    }
}
```

### 3.4 数据库设计

#### 3.4.1 下载任务表（download_tasks）

```sql
CREATE TABLE download_tasks (
    task_id TEXT PRIMARY KEY,
    title TEXT NOT NULL,
    bvid TEXT NOT NULL,
    cid INTEGER NOT NULL,
    avid INTEGER,
    ep_id INTEGER,

    -- 下载配置
    video_quality_id INTEGER DEFAULT 200,
    audio_quality_id INTEGER DEFAULT 30300,
    video_codec_id INTEGER DEFAULT 20,
    download_type INTEGER DEFAULT 1,  -- 1:视频+音频 2:仅视频 3:仅音频

    -- 下载信息
    media_type TEXT,  -- DASH/MP4/FLV
    download_path TEXT NOT NULL,
    file_name TEXT,
    file_path TEXT,
    total_size INTEGER DEFAULT 0,
    downloaded_size INTEGER DEFAULT 0,

    -- 状态信息
    status INTEGER DEFAULT 0,  -- 0:排队 1:下载中 2:暂停 3:失败 4:完成
    progress INTEGER DEFAULT 0,
    speed INTEGER DEFAULT 0,

    -- 时间信息
    create_time INTEGER NOT NULL,
    complete_time INTEGER,

    -- 附加信息
    cover_url TEXT,
    uploader TEXT,
    duration INTEGER,

    UNIQUE(bvid, cid, video_quality_id, audio_quality_id)
);
```

#### 3.4.2 下载文件分片表（download_chunks）

```sql
CREATE TABLE download_chunks (
    chunk_id INTEGER PRIMARY KEY AUTOINCREMENT,
    task_id TEXT NOT NULL,
    file_key TEXT NOT NULL,  -- video/audio
    chunk_index INTEGER NOT NULL,
    start_pos INTEGER NOT NULL,
    end_pos INTEGER NOT NULL,
    downloaded INTEGER DEFAULT 0,
    status INTEGER DEFAULT 0,  -- 0:未下载 1:下载中 2:已完成

    FOREIGN KEY(task_id) REFERENCES download_tasks(task_id)
);
```

### 3.5 UI设计建议

#### 3.5.1 视频详情页下载按钮

```
+------------------------------------------+
|  [封面图片]    视频标题                   |
|                UP主: xxx                 |
|                播放: xxx  弹幕: xxx      |
|                                          |
|  [播放] [下载] [收藏] [稍后再看] [分享] |
|                                          |
|  分P列表:                                |
|  [P1] xxx                                |
|  [P2] xxx                                |
+------------------------------------------+
```

#### 3.5.2 下载选项对话框

```
+------------------------------------------+
|          下载选项                         |
+------------------------------------------+
|  画质选择:                               |
|  [自动选择 ▼]                            |
|                                          |
|  音质选择:                               |
|  [自动选择 ▼]                            |
|                                          |
|  编码选择:                               |
|  [自动选择 ▼]                            |
|                                          |
|  附加内容:                               |
|  [✓] 弹幕    [✓] 字幕    [✓] 封面       |
|                                          |
|  预估大小: 1.2 GB                        |
|                                          |
|        [取消]        [开始下载]          |
+------------------------------------------+
```

#### 3.5.3 下载管理页面

```
+------------------+-----------------------+
|  [下载中 (3)]    |  任务1                |
|                  |  标题: xxx            |
|  [已完成 (5)]    |  进度: 45%   速度: 2MB/s |
|                  |  [暂停] [删除]        |
|                  |-----------------------+
|                  |  任务2                |
|                  |  标题: xxx            |
|                  |  进度: 78%   速度: 1.5MB/s |
|                  |  [暂停] [删除]        |
+------------------+-----------------------+
```

---

## 四、实施建议与注意事项

### 4.1 实施优先级

**第一阶段（核心功能）**：
1. 实现下载任务创建和管理
2. 实现基本的下载功能（单线程下载）
3. 实现下载管理页面
4. 实现播放已下载视频

**第二阶段（增强功能）**：
1. 支持多线程下载
2. 支持断点续传
3. 支持画质/音质/编码选择
4. 支持DASH格式下载

**第三阶段（完善功能）**：
1. 支持下载弹幕、字幕、封面
2. 支持视频合并（FFmpeg）
3. 支持下载限速
4. 支持批量下载

### 4.2 关键技术难点

#### 4.2.1 DASH格式下载

**难点**：
- 需要分别下载视频和音频流
- 需要使用FFmpeg合并
- FFmpeg库体积大

**解决方案**：
1. **优先方案**：下载已合并的MP4格式（设置fnval=16）
2. **备选方案**：集成FFmpeg Android库（增加APK体积约20MB）

#### 4.2.2 WBI签名

**难点**：
- 需要获取最新的WBI密钥
- 密钥会定期更新

**解决方案**：
- 复用项目中已有的`WbiSigner`类
- 定期更新密钥（可通过API获取）

#### 4.2.3 Cookie管理

**难点**：
- 下载需要登录Cookie
- Cookie可能过期

**解决方案**：
- 复用项目中已有的`CookieUtil`类
- 在下载前检查Cookie有效性

#### 4.2.4 存储权限

**难点**：
- Android 10+ 需要使用Scoped Storage
- 需要支持外置存储

**解决方案**：
1. **Android 10+**：使用`MediaStore` API保存到公共目录
2. **Android 9及以下**：使用传统文件API
3. **外置存储**：使用`Storage Access Framework`

### 4.3 性能优化建议

#### 4.3.1 下载性能

- 使用连接池复用HTTP连接
- 支持HTTP/2多路复用
- 合理设置分片大小（4-8MB）
- 限制并发下载数量（建议1-3个）

#### 4.3.2 数据库性能

- 使用事务批量写入
- 异步更新进度（避免频繁写入）
- 定期清理已完成任务的分片信息

#### 4.3.3 内存优化

- 使用流式下载（避免一次性加载大文件到内存）
- 及时释放已下载分片的内存
- 使用LRU缓存策略管理下载任务对象

### 4.4 兼容性考虑

#### 4.4.1 Android版本兼容

- **最低支持Android 4.x**：使用兼容API
- **Android 6.0+**：动态权限申请
- **Android 10+**：Scoped Storage
- **Android 11+**：强制Scoped Storage

#### 4.4.2 设备兼容

- **存储路径**：支持内置存储和外置SD卡
- **CPU架构**：如果使用FFmpeg，需要支持多种CPU架构（arm64-v8a, armeabi-v7a, x86等）
- **网络环境**：支持WiFi和移动网络，支持限速

### 4.5 安全性考虑

#### 4.5.1 数据安全

- **Cookie保护**：不将Cookie写入日志
- **文件权限**：设置合适的文件权限
- **路径安全**：防止路径遍历攻击

#### 4.5.2 网络安全

- **HTTPS**：强制使用HTTPS
- **证书校验**：校验服务器证书
- **域名校验**：校验域名白名单

---

## 五、总结

### 5.1 可行性结论

**✅ 整体可行性：高**

基于以上分析，在当前Android TV项目中移植B站视频下载功能是**完全可行**的，主要依据：

1. **API接口可复用**：项目中已有视频信息获取、WBI签名、Cookie管理等基础功能
2. **网络框架完备**：OkHttp3 + Retrofit框架成熟稳定
3. **数据库支持**：Android原生SQLite完全满足需求
4. **UI框架适配**：现有UI框架支持扩展新功能

### 5.2 关键优势

1. **代码可控**：二次开发项目，可以自由修改和扩展
2. **架构清晰**：现有代码结构清晰，便于集成新功能
3. **技术成熟**：下载功能技术方案成熟，有大量开源实现可参考

### 5.3 主要挑战

1. **DASH格式处理**：需要决定是否集成FFmpeg
2. **存储权限管理**：需要处理不同Android版本的权限差异
3. **性能优化**：需要在TV设备上保证流畅性
4. **测试验证**：需要在多种设备和网络环境下测试

### 5.4 实施建议

1. **采用渐进式开发**：先实现基础功能，再逐步完善
2. **优先MP4格式**：避免DASH格式的复杂性
3. **参考开源实现**：借鉴成熟的Android下载库（如FileDownloader、OkDownload等）
4. **重视用户体验**：在TV上操作方式需要简洁直观

### 5.5 预估工作量

| 功能模块 | 预估工作量 | 优先级 |
|---------|-----------|-------|
| 下载任务管理 | 3-5天 | 高 |
| 基本下载功能 | 5-7天 | 高 |
| 下载管理页面 | 3-5天 | 高 |
| 播放已下载视频 | 2-3天 | 高 |
| 画质音质选择 | 3-5天 | 中 |
| 断点续传 | 2-3天 | 中 |
| 多线程下载 | 3-5天 | 低 |
| DASH格式支持 | 5-7天 | 低 |
| FFmpeg合并 | 3-5天 | 低 |
| 弹幕字幕下载 | 2-3天 | 低 |

**总计**：核心功能约15-25天，完整功能约30-50天

---

## 六、附录

### 6.1 参考资源

1. **Bili23-Downloader源码**：`d:\code\mycodes\androidtv\bilibilitv1.6.6-repair-uiiang\参考\Bili23-Downloader`
2. **OkHttp官方文档**：https://square.github.io/okhttp/
3. **Android Storage Access Framework**：https://developer.android.com/guide/topics/providers/document-provider
4. **FFmpeg Android**：https://github.com/ArthurHub/Android-FFmpeg

### 6.2 相关文件路径

#### 参考项目关键文件

- 下载器核心：`src/util/download/downloader/downloader.py`
- 任务管理：`src/util/download/task/manager.py`
- 视频信息解析：`src/util/download/parse/video_info.py`
- URL解析：`src/util/download/downloader/parse_worker.py`
- CDN探测：`src/util/network/download_url.py`
- 画质音质映射：`src/util/common/data/media_info.py`

#### 当前项目关键文件

- 视频详情页：`mybv/java/classes/com/bilibili/tv/ui/video/VideoDetailActivity.java`
- 设置页面：`mybv/java/classes/com/bilibili/tv/ui/setting/SettingActivity.java`
- WBI签名：`mybv/java/classes/mybl/WbiSigner.java`
- Cookie管理：`mybv/java/classes/mybl/CookieUtil.java`
- 视频API：`mybv/java/classes/com/bilibili/tv/api/video/VideoApiParser.java`

---

**文档版本**: v1.0
**创建时间**: 2026-08-03
**最后更新**: 2026-08-03